# 开发日志 2026-08-22（第三天）

## 今日承接：昨日遗留任务处理

昨日日志（DEV_LOG_2026-08-21.md）的「明日规划」，今日逐项核对处理：

### 1. 精扫间隔（任务#1）—— 按用户决定保持 300s
- 用户表态「先不动，保持 300s」：精扫实测 45.9s（本次冷启动首轮），相对 300s 间隔仍有余量，但暂不缩间隔，先观察限速后再定。
- `config.py` 的 `DEEP_INTERVAL_SEC` 维持 300，未改动。

### 2. 观察新 reason 格式（任务#2）—— 已观察，待实盘新信号出现
- 当前 DB 仅 1 条信号（XAU/USDT long，已过期），是**旧格式** reason：
  `做多信号 - B级扳机（放量突破），量比 1.5351564772298012，止损 4563.05（2.0×ATR），目标 4623.26，盈亏比 2.5`
- 旧格式问题：量比未格式化（`1.5351564772298012`）+ 仍含已由 execution 结构字段展示的止损/目标/盈亏比冗余文本 → 在 Review.vue `reason.slice(0,40)` 会被截断成长数字，观感差。
- 新格式（`_build_reason` 已改 `:.2f` 且去除冗余）：`做多信号 - B级扳机（放量突破），量比 1.54`，短且干净。
- 结论：旧信号会随过期沉底自然淡出，属于**过渡期共存**问题；无需额外处理。待本轮实盘产生新信号后再确认卡片观感。

### 3. 前端优化
- **任务#4（易发现性）已落地**：`SignalFeed.vue` 把「看引擎过滤 N 条」从候选池折叠区**移到卡片标题下方、始终可见**（不受候选池折叠影响），并加了浅色背景便于点击；币种列表与否决列表解耦、各自独立展开。
- **任务#3（历史信号分组标题）**：**保留**。分隔线能清晰区分「有效信号」与「沉底的过期/止损信号」，信息不冗余，故不删除。

### 4. 服务与构建
- 后端已拉起：`http://localhost:8000`，调度器运行中（粗筛 60s / 精扫 300s / 信号监控 60s）。
- 前端重新构建（vite build）：新 hash `index-DTr3qOCh.js` / `index-CdS2Wcec.css`，`:8000` 已托管新构建产物。

## 持续观察（需长跑数据）
- **任务#5 限速**：本次启动仅 1 轮精扫（45.9s，0 信号），**无 429**；需长跑多轮后统计 weight/min 与 429 次数才能判断余量。
- **任务#6 僵尸币阈值**：当前候选池 166 币，含 meme（我踏马来了、FARTCOIN 等）与股票代币（TSLA/QQQ/NVDA/MSTR/SAMSUNG 等）；100 万 U 阈值合理性需实盘行情下进一步验证。
- **任务#7 列表长度**：当前仅 1 条过期信号，远未达 50 条上限；等信号频发后再看沉底分组的列表表现。

## M5 打磨：确定项落地（本日）

依 TECH_DESIGN §7 里程碑，M1-M4 已完成，本次补 M5 的**确定三项**；复盘统计按「信号兑现率（自动后验）」口径留作后续规划。

### 1. README.md（新建，项目根）
- 项目定位/核心原则（只辅助、不自动下单）、技术栈、前后端启动、定时任务、目录结构、settings.json 配置说明、免责声明。

### 2. 宏观静默内置事件表（新增 calendar 模块）
- `backend/app/calendar/macro.py`：
  - `estimate_builtin_events(now, months=2)`：按月规则估算未来常规事件（非农=每月第一个周五、CPI=每月 13 日、FOMC 每季度 + 议息发布会），时刻取当日 20:30 UTC，只保留未过去的。
  - `seed_builtin_events()`：宏事件表为空时写入内置事件（`source='builtin'`）。
- `backend/app/calendar/__init__.py`（空包）。
- `main.py` 启动在 `db.init_db()` 后调用 `seed_builtin_events()`。
- 验证：启动后 `/api/macro-events` 返回 4 条内置事件（2026-09 起；8 月已过事件被自动过滤）。静默判定沿用引擎查 db，内置事件参与静默；可在设置页修正/删除（新增为 manual）。

### 3. 参数持久化 settings.json（优先级 settings.json > config.py）
- `config.py`：新增 `load_settings_override()`（启动读取 `data/settings.json`，仅覆盖「大写且已存在」的键）+ `save_setting(key, value)`；模块导入即加载覆盖。
- `api/routes.py`：`PUT /api/settings/{key}` 增加 `config.save_setting(...)`，写盘持久化。
- 验证：`PUT MACRO_SILENCE_MINUTES=5` → `data/settings.json` 生成 `{"MACRO_SILENCE_MINUTES":5}`；重启后 `GET /api/settings` 返回 5（默认 15 被覆盖），证明启动加载生效；测试后已删除测试文件并恢复默认 15。

### 验证说明
- 后端已重启并运行：`http://localhost:8000`；宏事件 4 条、MACRO_SILENCE_MINUTES=15（默认）。
- `config.py` / `calendar/macro.py` 通过 `import` 验证（语法/依赖正常）。
- **环境限制**：`py12` 环境未装 `pytest`，本日无法运行后端单测（昨天 79/79 的测试环境不在当前会话可用）；改动为低风险增量，已用端到端（后端启动 + API 正常 + macro seed + settings 覆盖）验证。

## 备注
- 遗留任务的**代码可落地项（前端#4）已完成并构建上线**；#1 按用户决定保持；#2/#5/#6/#7 属观察类，交由长跑数据验证。
- M5 确定三项已落地：README、宏观静默内置表、参数持久化；复盘统计（信号兑现率）待后续规划。

## N0.1 动量确认放宽（第二阶段启动）

- 背景：实测 166 候选池中 **142（82%）** 卡在「扳机未触发（需 5m MACD 柱同向翻色）」，信号长期为 0 → 实盘无机会。
- 改动：
  - `indicators/engine.py`：指标快照新增 `macd_hist_streak`（MACD 柱当前符号的连续根数）。
  - `config.py`：新增 `TRIGGER_MOMENTUM_BARS = 3`（1=仅刚翻色原逻辑 / 3=默认放宽 / 5=激进）。
  - `signal/engine.py`：`_trigger` 的 5m 确认从「刚翻色」放宽为「同号连续根数 ≤ TRIGGER_MOMENTUM_BARS」。
- 验证（重启后首轮精扫，候选池 178）：「扳机未触发」**142 → 52**；「方向门未开」30 → 123（当前行情下方向门成为主要瓶颈，对应 N0.3 ADX 门槛可调）。
- 规划：`docs/PHASE2_PLAN.md` 更新至 v0.2（策略基线 + N0/N1 任务清单）。
- 待办：N0.2 扳机灵敏度档位（设置页 保守/标准/激进）→ N0.3 ADX 暴露（`ADX_TREND_TH` 设置页已可调）→ N0.4 波段入场 → N0.5 量能加分+vol targeting → N0.6 观察级信号；随后 N1 执行层（一键确认实盘）。

## N0.3 + N0.2（第二阶段继续）

- **N0.3 ADX 门槛调低**：`config.ADX_TREND_TH` 25→20。重启后方向门拦截 **123 → 114**，降幅有限 —— 诊断：方向门主要卡的是「三条件严格一致」（4h EMA55 / 1h 排列 / 4h MACD 零轴），ADX 只是其一；保持 20（小幅放行、质量损失小），不再深挖方向门结构。
- **N0.2 扳机灵敏度（最小版）**：`Settings.vue` 的 `EDITABLE_KEYS` 暴露 `TRIGGER_MOMENTUM_BARS`（1=保守原逻辑 / 3=标准默认 / 5=激进），前端重新构建并托管（`index-Z0UhQ9j1.js`）。下拉档位增强留待后续。
- 待办：N0.4 波段入场（1h/4h 信号路径）→ N0.5 量能加分+vol targeting → N0.6 观察级信号；随后 N1 执行层。

## ADX 三档实验（用户决策）

- `config.ADX_TREND_TH` 30（20→25→30 实测对比，方向门拦截 / 扳机拦截）：
  - **20**：方向门 114 / 扳机 59（最松，弱趋势也放行）
  - **25**：方向门 123 / 扳机 52（原默认）
  - **30**：方向门 138 / 扳机 36（当前，最严，只留强趋势）
- 规律：ADX 越高 → 方向门拦越多 → 进扳机的币越少 → 信号更少但更挑。当前保持 30；若实盘机会过少可回退 20/25（设置页可调）。

## N0.5 量能加分 + 波动率目标仓位

- **量能正向加分**（`signal/scorer.py`）：`score_signal` 新增 `volume_ratio` / `oi_change` 参数；放量+OI 增 +8（真突破）、单边 +4、缩量回踩（A 级蓄势）+4，并入置信度（min 100）。
- **波动率目标仓位**（`signal/engine.py` `_vol_factor` + `config`）：`position_factor = 费率系数 × 波动率系数`；`VOL_TARGET_ATR_PCT=0.008`，实际 ATR% 高于目标自动降仓（下限 `VOL_FACTOR_MIN=0.5`），低于目标不放大（上限 1.0）。
- 验证：`_vol_factor` 逻辑单测通过（0.5/100→1.0，1.6/100→0.5）；后端重启后参数生效。
- **里程碑：首轮精扫出现信号**（此前长期 0）：SKHYNIX/USDT 做多 conf=73（新信号）、XAU/USDT 做多 conf=90（含历史）——N0.1 动量放宽 + N0.3 ADX + N0.5 叠加后机会开始出现。

## N0.7 策略一：EMA 趋势跟踪（用户定制的第二策略路径）

- 新增 `backend/app/strategy/ema_trend.py`（纯函数）：
  - **入场**：趋势周期（默认 4h）EMA200 定方向且向上/走平 + 入场周期（默认 15m）EMA20>EMA50 排列 + 金叉/回踩再站上 + RSI>50 + 放量（>20 均量×1.2）+ 收盘确认；空头为镜像对称（用户提供条件）。
  - **三层出场**（monitor 判定，收盘价为准）：① 吊灯止损（持仓期最高/最低收盘 ∓ 3×ATR）；② EMA50 收盘破位（趋势失效即走）；③ 时间止损（48 根入场周期 K 线未创新高/新低）。
- 周期对可配置：`config.EMA_TREND_TIMEFRAMES = {"trend": "4h", "entry": "15m"}`（可切 1h+5m）；EMA 20/50/200；出场参数均进设置页。
- `SignalCard` 新增 `strategy` 字段（`short` / `ema_trend`），`ema_trend` 信号不按短线 45min 有效期作废（90 天，离场全由三层出场判定）；前端信号卡显示「EMA趋势」标签。
- 精扫：`scan/deep.py` 对候选池并行跑「现有引擎 + 策略一」，趋势周期 K 线不足 220 根时补拉（EMA200 预热）。
- 验证：import 全通过；`check_exit` 时间止损逻辑触发正确；重启后首轮精扫产出 **4 条 ema_trend 信号**（YB/SPX/JASMY/CYS 做多，回踩企稳 RSI 68-79 放量，conf=70）——策略一正常运行，信号频率显著提升。

## N1.1 执行层：币安交易账户接入

- 新增 `backend/app/executor/binance.py`（`BinanceExecutor`）：
  - ccxt（binanceusdm）封装：`fetch_balance` / `fetch_positions` / `create_order` / `cancel_order`
  - **默认 dry_run 纸面模式**（`BINANCE_DRY_RUN=True`），不真正下单仅模拟记录
  - 未配置 Key/Secret 时安全返回错误（不抛异常）
- `config.py` 新增：`BINANCE_API_KEY/SECRET`、`BINANCE_DRY_RUN`、`BINANCE_MAX_ORDER_USDT=100`、`BINANCE_DAILY_OPEN_LIMIT=5`、`BINANCE_DAILY_LOSS_LIMIT=0.05`
- 配置：`data/settings.json` 已写入用户提供的 `BINANCE_API_KEY`（Secret 留空待用户填写）；`data/` 在 .gitignore 中，不入库。
- **安全**：`GET /api/settings` 过滤 `BINANCE_API_KEY/SECRET`（已验证不外泄）。
- 验证：import 通过；Key 从 settings.json 加载；configured=False（缺 Secret）时余额查询安全返回错误；重启后服务正常、敏感字段不外泄。

## N1.2 一键执行（后端 + 凭据打通）

- **凭据修正**：用户提供的 `BHoyHr...` 实为 **API Key**、`bwn8szx...` 为 **Secret**（此前填反导致 -2015/-2008）。`data/settings.json` 已按 Key=BHoyHr / Secret=bwn8szx 修正。
- **IP 白名单**：诊断出 `-2015` 根因是币安 API 开了「只访问受信任IP」，白名单缺本机出口 IP（`2.27.156.235`，多服务确认）；用户已加入白名单。
- **连通验证**：`fetch_balance` 成功返回 `{total: 2.6059, free: 2.6059}`。
- `main.py` 挂载 `executor = BinanceExecutor()` 到 `app.state.executor`。
- `routes.py` 新增 `POST /api/signals/{id}/execute`：
  - 校验信号存在/未执行/状态可执行；拉真实余额算预算（`min(free, BINANCE_MAX_ORDER_USDT)`）折算数量；`create_order`（dry_run 下模拟）；创建本地持仓；置 `executed` 幂等标记。
  - 验证：dry-run 下单成功（BZ/USDT 做空，0.03 合约，持仓#4）；重复执行返回 409（幂等 OK）。
- 待办：N1.2 前端「确认执行」按钮 → N1.3 持仓自动风控 → N1.4 实盘复盘。

## N1.3 持仓自动风控

- **positions 表扩展**（`store/db.py` + `_migrate` 旧库补列）：新增 `strategy`、`signal_id`、`realized_pnl` 列。
- **风控门禁**（`routes.execute` 前置）：单日开仓上限（`BINANCE_DAILY_OPEN_LIMIT=5`）、单日亏损熔断（`BINANCE_DAILY_LOSS_LIMIT=0.05`，`db.sum_realized_pnl_today` 统计）。
- **自动平仓**（新增 `position/monitor.py` `PositionMonitor`，调度器 `position_monitor` 每 60s）：
  - 短线持仓：`position.manager.evaluate_stage` 三段式 → action=='exit' 即平仓；
  - 策略一持仓：`strategy.ema_trend.check_exit`（三层出场），并维护持仓期最高/最低收盘；
  - 平仓 = 反向市价单（dry_run 模拟），写入 `realized_pnl`、置 `closed`、更新关联信号 `stopped_out`。
- `main.py` 挂载 `position_monitor`，平仓广播 `position:update`。
- 验证：重启后 jobs 含 `position_monitor`；positions 表迁移成功（strategy/signal_id）；import 全通过。

## 8/26-8/27 两套策略收紧 + 信号结果跟踪 + 执行完善

### 套1（10层关卡）口径收紧
- 移除市场环境拦截层（短线代币与 BTC 此消彼长，不强制同向），环境仅用于打分加分（143a594）。
- 量比统一口径：当前 VOL ÷ 过去20根均量（原 MA7）（3ae3c98）。
- A级缩量定义：当前15m VOL < 20根均量 × 0.7（原 MA7）（cb9c0f8）。
- RSI 回升具体化：多头 <40 回升至 ≥40 或超卖 <30 拐头；空头 >60 回落或超买 >70 拐头（a5a978a）。
- 量能否决仅应用于 B 级突破（A级回踩蓄势豁免）；OI 变化率改 15m 对比 1 小时前（3d677c9）。
- 清算距离改用真实预估强平价：`estimate_liq_price = P×(1∓1/杠杆±MMR)`，杠杆3 + 维持保证金率0.5%，替代清算密集区估算（23a34ef）。

### 策略一 EMA 趋势 V3 收紧（33d3993）
- 修复 RSI 区间漏洞：多头 RSI 仅 50~68（原 32~68 会放偏空形态）。
- 增加 1h EMA20/50 中期确认层。
- 回踩须突破前20根高点；EMA200 斜率阈值 0.3%；放量 2.0×；距 EMA20 ≤ 2 ATR。
- 信号 id 加 `_ema` 后缀与套1区分；精扫对每币双策略并行落库。

### 信号结果跟踪（8121aa9）
- signals 卡 result 字段（exit_type: stop/trend/time/expired + hit_target）；`/api/signal-stats` 统计接口；Review 页结局统计。

### 执行层完善（193d9f2 等）
- 杠杆限制 1~5（默认3），前端确认执行弹窗：预算默认总余额50%可改、杠杆可选、显示止损/第一目标/70/30 拆分。
- 最小名义 5 USDT 校验（-4164 修复）；真单模式 BINANCE_DRY_RUN=false。
- WS 鉴权改为 main.py HTTP 中间件 + ws_endpoint 手动 query token 校验（3cdbfd7）。

### 待观察
- 收紧后精扫（候选池180币）单轮 0 信号，需多轮积累评估是否矫枉过正。

## 8/27 宏观静默扩展（用户需求：全部美国高影响力数据 + 持仓保护）

### 事件表扩展（calendar/macro.py 重写）
- 覆盖从 4 类扩到 11 类：非农/CPI/PPI/零售销售/耐用品/密歇根信心/ISM 制造业/ISM 非制造业/初请失业金(每周四)/季度 GDP/FOMC 决议/FOMC 鲍威尔发布会。
- 时间基点修正：原统一估算 20:30 UTC（错误）；现按美东真实时点换算——常规数据 8:30 ET、ISM/密歇根 10:00 ET、FOMC 14:00 ET + 发布会 14:30 ET，含夏令时规则（3月第二个周日~11月第一个周日 UTC-4，其余 UTC-5）。
- 启动改为**重播种**（reseed）：删除旧 builtin 按新规则重建，仅影响 builtin 不影响用户 manual 条目；用户仍可在设置页修正/删除。
- 公共函数 `in_silence_window()` / `next_macro_event()`：开仓静默与持仓保护共用同一判定。

### 已持仓静默保护（position/monitor.py `_macro_protect`，每持仓仅一次）
- 窗口内：收紧止损到现价 ± 0.5×ATR（`MACRO_SILENCE_STOP_ATR`，**只紧不松**：多头 max(原止损, 现价-0.5ATR)、空头 min(原止损, 现价+0.5ATR)），浮盈锁保本、浮亏限损，绝不放大风险。
- 可选减仓：`MACRO_SILENCE_REDUCE_PCT`（默认 0 = 只收紧不减仓；>0 时按比例 reduceOnly 市价部分平仓，剩余不足币种最小下单量则放弃减仓）。
- 交易所侧同步：撤旧 STOP_MARKET 单，按（可能已减仓的）新数量/新价重挂。
- positions 表新增 `macro_protected` 列（迁移补列）。

### 状态透出
- GET /api/status 新增 `macro_silence` / `next_macro_event`；前端 SignalFeed 顶部横幅：静默中红色警示"暂停新开仓、持仓已收紧止损"，非静默显示下一事件倒计时。
- 设置白名单新增 MACRO_SILENCE_STOP_ATR / MACRO_SILENCE_REDUCE_PCT（运行时可调）。

### 验证
- 时区换算 5 组边界（夏令时起止日）全部正确；窗口边界判定正确。
- `_macro_protect` 5 场景单测通过（scripts/test_macro_protect.py）。
- 后端重启重播种 15 条事件；/api/status 返回下一事件（今日初请 12:30Z）。

## 8/27 量比口径统一：20 周期均量 → 14 周期均量（用户指令"20改成14"）

- 新增 `config.VOL_MA_WINDOW = 14`（原散落硬编码 20）。
- `indicators/engine.py volume_metrics`：`vol_ma20` → `vol_ma14`，量比 = 当前VOL ÷ 14周期均量；保留 vol_ma7/vol_ma21 参考线。
- `signal/engine.py`：A级缩量判定改 `vol_ma14`（×0.7）；B级放量(1.5×)/量能否决(1.2×)/量能加分(+8)均基于量比自动跟随新口径。
- `strategy/ema_trend.py`：策略一放量基准 `_VOL_WINDOW` 改引用 `config.VOL_MA_WINDOW`（两套策略口径统一）。
- 阈值不变：ADX 20、放量1.5×、量能否决1.2×、缩量0.7×、量能加分放量+OI增+8。

### 测试套件修复（顺带，此前 56→79 项）
- 测试临时目录适配沙箱：`tempfile.mkdtemp` 目录 sqlite 打不开 → 改工作区内 `tests/_test_tmp`（每模块独立 db 文件 + 启动清理）。
- 同步过时测试：`test_pullback_level_a_long`（RSI 回升阈值 40 后 fixture 未同步）、`test_block_near_liquidation_zone`（适配真实预估强平价语义）、`test_bear_market_blocks_long` → `test_bear_market_softens_long`（市场环境不再硬拦截）、settings 测试改用白名单内 key、测试 app 补 executor state。
- 结果：**79 项全部通过**。

## 8/27 旱地拔葱重定义（用户指令：单根强 K 线直接视为强突破）

- **旧定义**：仅未收盘且运行>10 分钟 + 突破前高 1.5×ATR → exception（绑定"临近收盘快速突破"场景，无影线约束）。
- **新定义**（`config.CANDLE_EXCEPTION_ATR_MULT=2.0` / `CANDLE_EXCEPTION_WICK_RATIO=0.2`）：
  - 多头：单根 15m `(close−open) > 2×ATR` 且上影线 `(high−max(open,close)) < 0.2×实体`
  - 空头：`(open−close) > 2×ATR` 且下影线 `(min(open,close)−low) < 0.2×实体`
  - 满足即返回 exception（忽略实体/影线比例与收盘状态，不等待收盘），防冲高回落插针。
- 快照新增 `last_open` 键；删除 `_EXCEPTION_ELAPSED_MS` 与旧 1.5×ATR 逻辑。
- 测试：新增多头/空头旱地拔葱 + 上影过长不触发共 3 项，**81 项全过**。

## 8/27 评分表重构（用户定稿：明确子项得分 + 通过线 70 + 封顶 100）

scorer.py 全面重写，每个子项给出明确得分条件（详见模块 docstring）：
- 市场环境 20：费率正常 +5 ｜ OI 与价格同向 +5（增仓≥1% 且价格沿持仓方向）｜ 无重大事件 +5（距最近事件>4h）｜ 波动率适中 +5（15m 带宽/中位 0.5~1.5）
- 趋势强度 30：4h EMA55 向上 +10（新增快照字段 ema55_slope_pct，斜率>0.05%）｜ 1h EMA7>21 +10 ｜ ADX 按程度 +10/6/2（≥25/20~25/<20）
- 时机 25：A级回踩 +15 ｜ B级突破 +10 ｜ 5m MACD 同向刚启动 +5（streak=1 刚翻色，2~3 延续）
- 风控 25：盈亏比≥2 +10（≥2.5 满）｜ 止损距离合理 +10（liq_dist_atr≥2 满）｜ 费率安全 +5
- 量能加分：放量+OI增 +8 ｜ 放量或OI单增 +4 ｜ 单边趋势缩量回踩 +4（非单边 +2）
- 通过线 config.SCORE_PASS=70（原 50）；min(score,100) 封顶防失真。
- engine.evaluate 传入 direction/risk_reward/macd_streak/next_event_mins 新参数。
- 测试：新增封顶 100 用例 + 更新 scorer 高低分用例，**82 项全过**。

## 8/27 放量观察 + 评分/关卡详情可视化（用户指令）

- 通过线 70 → 60（SCORE_PASS，信号过少）。
- 【临时放开】C 级扳机（RSI 穿越 50 无量能，原"只观察"拦截）→ config.TRIGGER_C_LEVEL_ALLOW=True，白名单可随时在设置页关闭。
- 评分明细：scorer 新增 score_breakdown() 返回 {total, pass_line, items:[{cat,name,score,max,note}]}；engine 存入 SignalCard.score_detail。
- 关卡详情：engine 新增 levels_detail（市场环境/方向门/扳机/量能/风控/K线形态/宏观 各关键判定数值），存入 SignalCard.levels_detail。
- 前端 SignalCard：点击分数弹窗显示评分项明细（按类别分组+合计+通过线）；"详情"入口弹窗显示各关卡判定值；confClass 阈值 70→60。
- 验证：82 项测试全过；手动深扫放出 2 个信号（ALAB 多 82 分 / SOXS 空 75 分），score_detail/levels_detail 正常落库。

## 8/27 确认执行弹窗秒开（用户反馈等待太久）

- 原逻辑：点击「确认执行」先 await api.account()（币安余额查询走代理慢）成功后才开弹窗 → 感知卡顿。
- 改为：点击立即弹窗（秒开），余额后台拉取，拉到后回填默认预算（总余额50%）；余额加载中显示"余额获取中…可手动输入预算"；拉取失败不阻塞弹窗。

## 8/27 默认预算改为全部余额（用户指令）

- config.EXEC_DEFAULT_BUDGET_PCT 0.5 → 1.0（默认预算 = 全部余额；白名单新增可运行时调回）。
- 前端确认弹窗：默认预算 = 全部余额，提示文案同步（"默认 = 全部余额（当前 X USDT）"）。
- 原因：余额 11.07U 时 50%→市价腿70% 仅 3.88U < 5U 币安最小名义，导致 400。
- 重启后首轮深扫出信号：MORPHO/USDT long 89 分（B级放量突破 量比2.71）。

## 8/27 实盘下单链路修复（双向持仓模式 -4061 等连环问题）

### 根因与修复（连续 3 个 bug，均已实盘验证）
1. **-4061（双向持仓模式）**：账户 dualSidePosition=True，下单必须带 positionSide。executor 新增 `_dual_position_mode()`（探测+缓存）与 `_order_params()`；开仓 buy→LONG/sell→SHORT 自动；**平仓/止损单由调用方传持仓侧**（平多=sell 但 positionSide=LONG）。
2. **-1106（双向模式多余 reduceOnly）**：双向模式下 STOP_MARKET/平仓单 positionSide 已限定持仓侧，币安不接受 reduceOnly（"sent when not required"）→ 双向模式不再带 reduceOnly，单向模式保留。
3. **-4164（限价腿名义不足）**：70/30 拆分下限价腿（30%预算）常 <5U 最小名义被币安拒 → 限价腿名义 <5U 时并入市价腿。
4. **500 pf NameError**：execute 返回 dict 引用未定义变量 `pf` → 改用 exec_plan.position_factor（此前每次走到 return 必 500）。
5. **fetch_positions 方向误判**：双向模式下 ccxt contracts 可能是绝对值 → 改用 `side` 字段。

### 实盘验证（11U 余额）
- VVV 做多：market 0.3 成交 → 修复后限价腿并入、无 -4164/-1106/500。
- SOXS 做空：完整流程成功（market 0.16 @45.68 + STOP_MARKET 止损单挂上）。
- 用户手动补仓 VVV +1.11 → 系统止损单从 0.3 更新为覆盖全部 1.41（2000001389722649），DB qty 同步。
- 82 项测试全过。

## 8/27 关闭临时放开的 C 级扳机（用户指令）

- TRIGGER_C_LEVEL_ALLOW True → False：C 级（RSI 穿越 50 无量能）恢复"只观察"拦截，信号只保留 A/B 级。
- 设置页白名单保留该开关，需要时随时可临时放开。

## 8/27 清空 C 级扳机信号（用户指令）

- 删除全部 3 条 trigger_level='C' 信号（ALAB/TAO 未执行直接删；SOXS 已执行开仓 → 删信号并置空持仓 signal_id，持仓与止损单保留继续自动风控）。
- 剩余信号仅 A/B 级（MORPHO 89 / VVV 89）；持仓 VVV long 1.41、SOXS short 0.16 均带止损单。

## 8/27 候选池成交额门槛提高（用户指令）

- CANDIDATE_MIN_QUOTE_VOLUME 100万 → 500万 USDT（24h 成交额 <500万 不入池，自选除外）。

## 8/27 隐藏离场/过期信号（用户指令）

- 前端 SignalFeed 不再展示"历史信号（已离场/过期）"分组；只显示有效信号，全无效时显示空提示。

## 8/27 涨跌家数显示 + 宏观事件北京时间（用户指令）

- 市场环境面板：涨跌家数比 → 涨数:跌数（后端 market_env 增加 up_count/down_count，前端显示"涨 X:跌 Y"）。
- 设置页宏观事件时间：统一以北京时间（UTC+8）展示（fmtEventTime 转 UTC+8 并标注"北京时间"）。

## 8/27 涨跌 0:0 修复（币安网络失败误报）

- 根因：代理 127.0.0.1:7892 到币安 fapi 连接失败（连续数小时），fetch_24h_tickers 拉取失败返回空 → 误报 0:0。
- 修复：拉取失败时 up_count/down_count 置 None，前端显示"-"（未知），网络恢复后自动显示真实涨跌数。

## 8/27 杠杆未生效诊断 + 自动平仓空转修复（用户反馈）

### 杠杆"没生效"根因（-2028）
- 实测 set_leverage 返回 -2028 "Leverage is smaller than permitted: insufficient margin balance"：
  **币安拒绝调低杠杆**——降低杠杆会增加保证金需求，账户余额不足时（如 ZRO 12 合约全仓按 3x 需保证金超余额）保持原杠杆。代码链路（execute 里 set_leverage 失败会 502 中止）本身无 bug，是账户保证金条件。
- 补充：positions 表新增 leverage 列（execute 写入开仓杠杆），持仓页显示杠杆倍数，方便用户确认。

### 自动平仓空转（-2022）修复
- 现象：用户手动平仓后币安无实际持仓，DB 仍 open，position monitor 每分钟发 reduceOnly 平仓单被 -2022 拒（日志刷屏）。
- 修复：executor.fetch_position(symbol) 单币持仓查询；_close 平仓前先校验币安实际仓位——无仓位直接同步关闭 DB（不再空转），方向不符跳过，有仓位以币安实际数量平仓。
- 已同步清理：CRWD/SOXS/VVV 3 条幽灵持仓关闭；ZRO 币安 12.0 保留。

## 8/27 杠杆参与下单名义（用户反馈：杠杆没起作用，按账户数字下单）

- 原逻辑：下单金额 = 预算（全部余额），杠杆只设置币安侧，**不参与下单数量** → 名义 = 余额，杠杆形同虚设。
- 修复：下单名义 = 保证金预算 × 杠杆（预算默认全部余额；名义 = 余额 × 杠杆，保证金占用 = 名义/杠杆 = 预算）。
- execute 返回增加 leverage / notional_usdt；前端弹窗提示"预算=保证金，名义=预算×杠杆"。
- 例：余额 11U × 3x → 下单名义 33U（数量 = 33×0.7/价格），保证金占用 11U。

## 8/28 自动下单模式（用户指令：默认直接下单 10x/5U + 单币种持仓）

1. **自动下单**：SignalMonitor.check 每 60s 先检查——无持仓时自动执行最早的未执行信号（复用 execute 核心 _execute_impl，默认参数 10x/5U），执行/跳过均记日志。
2. **默认参数**：BINANCE_MAX_LEVERAGE/DEFAULT_LEVERAGE 5→10；新增 EXEC_DEFAULT_BUDGET_USDT=5（固定本金 5U，替代全部余额）；前端弹窗默认 10x/5U、上限 10。
3. **单币种限制**：_execute_impl 开头校验——账户已有 open 持仓时拒绝新开仓（自动/手动统一限制，"同一时间只持一个币种"）。
4. 重构：execute 路由核心抽为 _execute_impl（ExecuteError 带状态码），路由与自动下单共用。

## 9/1 关闭自动下单（用户指令）

- 新增总开关 AUTO_EXECUTE_ENABLED=False（白名单可配置）：SignalMonitor 跳过自动执行，仅保留手动「确认执行」。
- 信号仍正常生成/展示；需要时改回 True 或设置页开启。

## 9/1 策略三：标的启动感知（框架版，用户定制）

- 新模块 `backend/app/strategy/launch_sense.py`：5 指标框架——成交量爆发/波动率扩张/均线/资金面质变/多周期共振（量化公式待用户提供，当前占位 False）。
- 组合规则：默认全部 5 指标通过才触发（LAUNCH_SENSE_MIN_INDICATORS=5）。
- 集成：deep.py `_scan_launch_sense` 每币评估，信号 id 加 `_ls` 后缀，去重/冷却复用；**不打分**（confidence=0）。
- 前端 SignalCard：launch_sense 卡**橙色渐变背景**+「启动感知」标签+🚀 启动徽标；5 指标状态直览（✓/✗/待定）；隐藏执行参数/一键执行区；详情弹窗展示指标判定值。
- 82 项测试全过；当前公式未定义，策略不触发信号，等用户给 5 指标公式后填充。

## 9/1 启动感知策略三层实现（用户详细设计）

三层漏斗（只做多，底部启动）：
- **L1 日线定方向**：价 < MA180 且 (价−180低)/(180高−180低) < 50%
- **L2 1h 确认**：BIAS = (收盘−MA24)/MA24 ∈ [3%, 8%]（LS_BIAS_MIN/MAX）
- **L3 5m 执行扳机**（三条件同时满足）：
  ① 量能爆发：Taker Buy Volume 3 根维持高位（Min>20均量×1.5 且 Min/Max≥0.85）——新增 fetch_ohlcv_taker（币安 klines 第9列）
  ② 波动率扩张：BOLL(20,2) 带宽 < 60根均值×60% + ATR_NOW(含实时)比12根ATR_BASE跳升>50% + 价>当前BOLL上轨
  ③ 均线抬头：A价<MA99×1.08 且 B价>MA7&MA25 且 C金叉 且 D MA7斜率>1%；**严格模式**同根验证，**激进模式**（LS_AGGRESSIVE）C 可提前一根
- 均线/量能用已收盘序列（t=最后一根已收盘 5m，避开未来函数）；ATR_NOW/上轨用含实时价序列。
- 参数全部 config 化（LS_*）；三层全过 → 橙色启动信号（不打分）。
- 测试：L1/L2/量能精确数据通过；evaluate 组装（mock 全 pass 触发 / 单子失败不触发）验证通过；82 项全过。
- 修复：taker K线 market_id 用 ccxt market() 官方 id（中文名币避免 -1121）。

## 9/1 启动感知改实时监听模式（用户指令：监听5m K线 + 仅L1+L2小池）

- **架构**：deep scan（5分钟）只做 **L1日线+L2 1h 预筛**（launch_pool 小池）；新增 **LaunchSenseWatcher**（launch_watch.py）订阅小池币种 **5m kline 收盘流**（币安 fstream 组合流，websockets 16 + proxy），每根 K 线**收盘瞬间**实时评估 **L3 三子条件**（量能/波动/均线），全过即爆信号（橙色卡，不打分）。
- 预筛：launch_sense 拆出 `prefilter`(L1+L2) / `check_l3`(L3)；deep.scan 维护 launch_pool，on_scan_complete 同步给 watcher（池变化触发重连）。
- 信号：复用去重/冷却/卡构造（id `_ls`），WS 广播 signal:new；status 增加 launch_pool 透出。
- 验证：L1+L2 预筛 157 候选通过 5 个（1000BONK/GWEI/OP/STABLE/TAC）；82 测试全过；监听线程启动正常。

## 9/2 启动感知全在线监听（用户指令：L1+L2+L3 全部在线）

- **双流监听**（launch_watch.py 重写）：
  - 1h 流：订阅候选池全部币 1h kline 收盘 → 实时评估 **L1(日线)+L2(BIAS)** → 维护 L3 观察集（进入/移出）
  - 5m 流：订阅 L3 观察集 5m kline 收盘 → 实时评估 **L3 三子条件** → 全过爆信号
- 候选池由 deep scan 每 5 分钟同步（update_watchlist）；**新增币立即初始 L1+L2 评估**（避免等 1h 收盘冷启动空窗）。
- deep scan 移除预筛（不再拉 1d/taker，省请求）；status 广播 launch_pool（L3 观察集）。
- 验证：1h 流连接 169 币正常；82 测试全过。
